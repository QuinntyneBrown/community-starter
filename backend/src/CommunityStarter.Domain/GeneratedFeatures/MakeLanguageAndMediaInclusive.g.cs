// <auto-generated />
#nullable enable
using CommunityStarter.Domain.Common;

namespace CommunityStarter.Domain.GeneratedFeatures;

public sealed record MakeLanguageAndMediaInclusiveRequest(
    Guid SubjectId,
    string Action,
    long ExpectedVersion,
    Guid? CommunityId,
    IReadOnlyDictionary<string, string> Values)
{
    public FeatureInput ToInput() => new(SubjectId, Action, ExpectedVersion, CommunityId, Values);
}

public sealed record MakeLanguageAndMediaInclusiveRecord(
    Guid Id,
    Guid? CommunityId,
    string State,
    long Version,
    bool IsAuthorized,
    bool IsAvailable)
{
    public FeatureState ToState() => new(Id, CommunityId, State, Version, IsAuthorized, IsAvailable);
}

public sealed record MakeLanguageAndMediaInclusiveDecision(
    string RequirementId,
    FeatureDecisionKind Kind,
    string Code,
    string ResultingState,
    long ResultingVersion)
{
    public bool IsAccepted => Kind == FeatureDecisionKind.Accepted;

    public static MakeLanguageAndMediaInclusiveDecision From(FeatureDecision decision) =>
        new(decision.RequirementId, decision.Kind, decision.Code, decision.ResultingState, decision.ResultingVersion);
}

public sealed class MakeLanguageAndMediaInclusivePolicy
{
    public MakeLanguageAndMediaInclusiveDecision LocalizeEveryUserFacingContract(
        MakeLanguageAndMediaInclusiveRecord record,
        MakeLanguageAndMediaInclusiveRequest request) =>
        MakeLanguageAndMediaInclusiveDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-UXDS-014", record.ToState(), request.ToInput()));

    public MakeLanguageAndMediaInclusiveDecision RenderLocaleAndTimeZoneValuesConsistently(
        MakeLanguageAndMediaInclusiveRecord record,
        MakeLanguageAndMediaInclusiveRequest request) =>
        MakeLanguageAndMediaInclusiveDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-UXDS-015", record.ToState(), request.ToInput()));

    public MakeLanguageAndMediaInclusiveDecision RequireAccessibleMediaAlternatives(
        MakeLanguageAndMediaInclusiveRecord record,
        MakeLanguageAndMediaInclusiveRequest request) =>
        MakeLanguageAndMediaInclusiveDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-UXDS-017", record.ToState(), request.ToInput()));
}

public static class MakeLanguageAndMediaInclusiveDescriptor
{
    public const string Subsystem = "experience-design-system";
    public const string Slug = "make-language-and-media-inclusive";
    public const string Title = "Make language, locale, and media inclusive";

    public static IReadOnlyList<FeatureOperationDescriptor> Operations { get; } =
    [
        new("L2-UXDS-014", "LocalizeEveryUserFacingContract"),
        new("L2-UXDS-015", "RenderLocaleAndTimeZoneValuesConsistently"),
        new("L2-UXDS-017", "RequireAccessibleMediaAlternatives")
    ];
}
