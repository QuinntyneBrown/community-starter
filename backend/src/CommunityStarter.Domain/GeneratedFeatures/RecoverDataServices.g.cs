// <auto-generated />
#nullable enable
using CommunityStarter.Domain.Common;

namespace CommunityStarter.Domain.GeneratedFeatures;

public sealed record RecoverDataServicesRequest(
    Guid SubjectId,
    string Action,
    long ExpectedVersion,
    Guid? CommunityId,
    IReadOnlyDictionary<string, string> Values)
{
    public FeatureInput ToInput() => new(SubjectId, Action, ExpectedVersion, CommunityId, Values);
}

public sealed record RecoverDataServicesRecord(
    Guid Id,
    Guid? CommunityId,
    string State,
    long Version,
    bool IsAuthorized,
    bool IsAvailable)
{
    public FeatureState ToState() => new(Id, CommunityId, State, Version, IsAuthorized, IsAvailable);
}

public sealed record RecoverDataServicesDecision(
    string RequirementId,
    FeatureDecisionKind Kind,
    string Code,
    string ResultingState,
    long ResultingVersion)
{
    public bool IsAccepted => Kind == FeatureDecisionKind.Accepted;

    public static RecoverDataServicesDecision From(FeatureDecision decision) =>
        new(decision.RequirementId, decision.Kind, decision.Code, decision.ResultingState, decision.ResultingVersion);
}

public sealed class RecoverDataServicesPolicy
{
    public RecoverDataServicesDecision CreateAndVerifyProtectedBackups(
        RecoverDataServicesRecord record,
        RecoverDataServicesRequest request) =>
        RecoverDataServicesDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-OPER-012", record.ToState(), request.ToInput()));

    public RecoverDataServicesDecision ProveRecoveryObjectivesThroughRestoreDrills(
        RecoverDataServicesRecord record,
        RecoverDataServicesRequest request) =>
        RecoverDataServicesDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-OPER-013", record.ToState(), request.ToInput()));

    public RecoverDataServicesDecision ReconcileDerivedStateAfterRecovery(
        RecoverDataServicesRecord record,
        RecoverDataServicesRequest request) =>
        RecoverDataServicesDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-OPER-014", record.ToState(), request.ToInput()));
}

public static class RecoverDataServicesDescriptor
{
    public const string Subsystem = "operations-reliability";
    public const string Slug = "recover-data-services";
    public const string Title = "Data and service can be recovered";

    public static IReadOnlyList<FeatureOperationDescriptor> Operations { get; } =
    [
        new("L2-OPER-012", "CreateAndVerifyProtectedBackups"),
        new("L2-OPER-013", "ProveRecoveryObjectivesThroughRestoreDrills"),
        new("L2-OPER-014", "ReconcileDerivedStateAfterRecovery")
    ];
}
