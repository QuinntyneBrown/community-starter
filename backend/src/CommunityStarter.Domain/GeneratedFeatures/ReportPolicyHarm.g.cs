// <auto-generated />
#nullable enable
using CommunityStarter.Domain.Common;

namespace CommunityStarter.Domain.GeneratedFeatures;

public sealed record ReportPolicyHarmRequest(
    Guid SubjectId,
    string Action,
    long ExpectedVersion,
    Guid? CommunityId,
    IReadOnlyDictionary<string, string> Values)
{
    public FeatureInput ToInput() => new(SubjectId, Action, ExpectedVersion, CommunityId, Values);
}

public sealed record ReportPolicyHarmRecord(
    Guid Id,
    Guid? CommunityId,
    string State,
    long Version,
    bool IsAuthorized,
    bool IsAvailable)
{
    public FeatureState ToState() => new(Id, CommunityId, State, Version, IsAuthorized, IsAvailable);
}

public sealed record ReportPolicyHarmDecision(
    string RequirementId,
    FeatureDecisionKind Kind,
    string Code,
    string ResultingState,
    long ResultingVersion)
{
    public bool IsAccepted => Kind == FeatureDecisionKind.Accepted;

    public static ReportPolicyHarmDecision From(FeatureDecision decision) =>
        new(decision.RequirementId, decision.Kind, decision.Code, decision.ResultingState, decision.ResultingVersion);
}

public sealed class ReportPolicyHarmPolicy
{
    public ReportPolicyHarmDecision ApplyVersionedPolicyPrecedence(
        ReportPolicyHarmRecord record,
        ReportPolicyHarmRequest request) =>
        ReportPolicyHarmDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-SAFE-001", record.ToState(), request.ToInput()));

    public ReportPolicyHarmDecision SubmitASafeReport(
        ReportPolicyHarmRecord record,
        ReportPolicyHarmRequest request) =>
        ReportPolicyHarmDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-SAFE-002", record.ToState(), request.ToInput()));

    public ReportPolicyHarmDecision ProtectReportersAndLimitAbuse(
        ReportPolicyHarmRecord record,
        ReportPolicyHarmRequest request) =>
        ReportPolicyHarmDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-SAFE-003", record.ToState(), request.ToInput()));

    public ReportPolicyHarmDecision PreserveABoundedPriorViewReportingPath(
        ReportPolicyHarmRecord record,
        ReportPolicyHarmRequest request) =>
        ReportPolicyHarmDecision.From(FeaturePolicyEvaluator.Evaluate(
            "L2-SAFE-016", record.ToState(), request.ToInput()));
}

public static class ReportPolicyHarmDescriptor
{
    public const string Subsystem = "moderation-trust-safety";
    public const string Slug = "report-policy-harm";
    public const string Title = "Safe policy reporting";

    public static IReadOnlyList<FeatureOperationDescriptor> Operations { get; } =
    [
        new("L2-SAFE-001", "ApplyVersionedPolicyPrecedence"),
        new("L2-SAFE-002", "SubmitASafeReport"),
        new("L2-SAFE-003", "ProtectReportersAndLimitAbuse"),
        new("L2-SAFE-016", "PreserveABoundedPriorViewReportingPath")
    ];
}
