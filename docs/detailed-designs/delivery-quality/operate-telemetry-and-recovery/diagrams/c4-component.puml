@startuml
!include <C4/C4_Component>
LAYOUT_WITH_LEGEND()
title Components — Operate the starter with actionable telemetry and recovery

Container(frontend, "Engineering Workbench", "CLI and repository tools", "Presents state and sends typed intent")
Container_Boundary(backend, "Delivery Control Plane") {
  Component(endpoint, "OperateTelemetryAndRecoveryEndpoint", "pipeline entry point", "Authenticates and dispatches feature requests")
  Component(handler, "OperateTelemetryAndRecoveryHandler", "Application service", "Coordinates authorized state and one unit of work")
  Component(policy, "OperateTelemetryAndRecoveryPolicy", "Domain policy", "Evaluates traced behavior against current state")
  Component(repository, "IOperateTelemetryAndRecoveryRepository", "Persistence port", "Loads scoped state and commits conditionally")
  Component(projector, "OperateTelemetryAndRecoveryProjector", "Post-commit component", "Processes committed outcomes idempotently")
  ComponentDb(store, "Evidence Registry", "Versioned artifact metadata", "Stores state, versions, outcomes, and checkpoints")
}

Rel(frontend, endpoint, "Submits OperateTelemetryAndRecoveryRequest", "Typed contract")
Rel(endpoint, handler, "Dispatches request")
Rel(handler, policy, "Evaluates current state")
Rel(handler, repository, "Loads and commits")
Rel(repository, store, "Reads and writes", "Transactional adapter")
Rel(handler, projector, "Publishes committed outcome", "Durable handoff")
Rel(projector, store, "Checkpoints processing", "Idempotent adapter")
@enduml
