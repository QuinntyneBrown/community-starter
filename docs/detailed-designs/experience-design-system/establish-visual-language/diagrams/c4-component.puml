@startuml
!include <C4/C4_Component>
LAYOUT_WITH_LEGEND()
title Components — Establish one canonical cross-surface visual language

Container(frontend, "Component Workbench", "Angular and Storybook", "Presents state and sends typed intent")
Container_Boundary(backend, "Design Validation Pipeline") {
  Component(endpoint, "EstablishVisualLanguageEndpoint", "validation entry point", "Authenticates and dispatches feature requests")
  Component(handler, "EstablishVisualLanguageHandler", "Application service", "Coordinates authorized state and one unit of work")
  Component(policy, "EstablishVisualLanguagePolicy", "Domain policy", "Evaluates traced behavior against current state")
  Component(repository, "IEstablishVisualLanguageRepository", "Persistence port", "Loads scoped state and commits conditionally")
  Component(projector, "EstablishVisualLanguageProjector", "Post-commit component", "Processes committed outcomes idempotently")
  ComponentDb(store, "Design Artifact Registry", "Versioned files", "Stores state, versions, outcomes, and checkpoints")
}

Rel(frontend, endpoint, "Submits EstablishVisualLanguageRequest", "Typed contract")
Rel(endpoint, handler, "Dispatches request")
Rel(handler, policy, "Evaluates current state")
Rel(handler, repository, "Loads and commits")
Rel(repository, store, "Reads and writes", "Transactional adapter")
Rel(handler, projector, "Publishes committed outcome", "Durable handoff")
Rel(projector, store, "Checkpoints processing", "Idempotent adapter")
@enduml
