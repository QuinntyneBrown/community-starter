@startuml
!include <C4/C4_Component>
LAYOUT_WITH_LEGEND()
title Components — Provide disciplined Angular component contracts

Container(frontend, "Component Workbench", "Angular and Storybook", "Presents state and sends typed intent")
Container_Boundary(backend, "Design Validation Pipeline") {
  Component(endpoint, "ProvideAngularComponentContractsEndpoint", "validation entry point", "Authenticates and dispatches feature requests")
  Component(handler, "ProvideAngularComponentContractsHandler", "Application service", "Coordinates authorized state and one unit of work")
  Component(policy, "ProvideAngularComponentContractsPolicy", "Domain policy", "Evaluates traced behavior against current state")
  Component(repository, "IProvideAngularComponentContractsRepository", "Persistence port", "Loads scoped state and commits conditionally")
  Component(projector, "ProvideAngularComponentContractsProjector", "Post-commit component", "Processes committed outcomes idempotently")
  ComponentDb(store, "Design Artifact Registry", "Versioned files", "Stores state, versions, outcomes, and checkpoints")
}

Rel(frontend, endpoint, "Submits ProvideAngularComponentContractsRequest", "Typed contract")
Rel(endpoint, handler, "Dispatches request")
Rel(handler, policy, "Evaluates current state")
Rel(handler, repository, "Loads and commits")
Rel(repository, store, "Reads and writes", "Transactional adapter")
Rel(handler, projector, "Publishes committed outcome", "Durable handoff")
Rel(projector, store, "Checkpoints processing", "Idempotent adapter")
@enduml
