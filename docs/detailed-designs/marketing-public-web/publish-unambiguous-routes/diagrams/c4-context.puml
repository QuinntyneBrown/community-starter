@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Publish marketing and application routes without ambiguity

Person(actor, "Visitor", "Reads public content and chooses an available application action")
System(system, "Community Public Web", "Static public content, Community previews, and application navigation")
System_Ext(external, "CDN and Search Crawlers", "Public caching, routing, and indexing systems")

Rel(actor, system, "Uses for publish marketing and application routes without ambiguity", "HTTPS or local command")
Rel(system, external, "Invokes for eligible external work", "Purpose-bound contract")
@enduml
