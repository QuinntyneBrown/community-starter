@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Recipient-controlled attention

Person(actor, "Account Holder", "Receives and controls eligible Notifications")
System(system, "Community Platform", "Community participation, administration, and safety system")
System_Ext(external, "Email Delivery Provider", "Attempts privacy-safe external email Delivery")

Rel(actor, system, "Uses for recipient-controlled attention", "HTTPS or local command")
Rel(system, external, "Invokes for eligible external work", "Purpose-bound contract")
@enduml
