@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Reach first Community value

Person(actor, "New Account Holder", "Completes onboarding and reaches a first Community")
System(system, "Community Platform", "Community participation, administration, and safety system")
System_Ext(external, "Notification Delivery", "Delivers eligible invitation and onboarding actions")

Rel(actor, system, "Uses for reach first Community value", "HTTPS or local command")
Rel(system, external, "Invokes for eligible external work", "Purpose-bound contract")
@enduml
