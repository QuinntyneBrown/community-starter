@startuml
!include <C4/C4_Component>
LAYOUT_WITH_LEGEND()
title Components — Protect Community data and persistence integrity

Container(frontend, "Engineering Workbench", "CLI and repository tools", "Presents state and sends typed intent")
Container_Boundary(backend, "Delivery Control Plane") {
  Component(endpoint, "ProtectDataPersistenceIntegrityEndpoint", "pipeline entry point", "Authenticates and dispatches feature requests")
  Component(handler, "ProtectDataPersistenceIntegrityHandler", "Application service", "Coordinates authorized state and one unit of work")
  Component(policy, "ProtectDataPersistenceIntegrityPolicy", "Domain policy", "Evaluates traced behavior against current state")
  Component(repository, "IProtectDataPersistenceIntegrityRepository", "Persistence port", "Loads scoped state and commits conditionally")
  Component(projector, "ProtectDataPersistenceIntegrityProjector", "Post-commit component", "Processes committed outcomes idempotently")
  ComponentDb(store, "Evidence Registry", "Versioned artifact metadata", "Stores state, versions, outcomes, and checkpoints")
}

Rel(frontend, endpoint, "Submits ProtectDataPersistenceIntegrityRequest", "Typed contract")
Rel(endpoint, handler, "Dispatches request")
Rel(handler, policy, "Evaluates current state")
Rel(handler, repository, "Loads and commits")
Rel(repository, store, "Reads and writes", "Transactional adapter")
Rel(handler, projector, "Publishes committed outcome", "Durable handoff")
Rel(projector, store, "Checkpoints processing", "Idempotent adapter")
@enduml
