@startuml
!include <C4/C4_Context>
LAYOUT_WITH_LEGEND()
title System Context — Teams can specialize the starter safely

Person(actor, "Starter Adopter", "Configures and specializes a clean repository checkout")
System(system, "Community Starter", "Reproducible development, specialization, and repository guidance system")
System_Ext(external, "Package Registries and Local Providers", "Toolchain, package, and development dependency services")

Rel(actor, system, "Uses for teams can specialize the starter safely", "HTTPS or local command")
Rel(system, external, "Invokes for eligible external work", "Purpose-bound contract")
@enduml
